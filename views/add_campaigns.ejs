
<% include ./partials/topbar %> 
<% include ./partials/sidebar %>
 <div class="container-fluid page-body-wrapper">
      <div class="main-panel">
        <div class="content-wrapper">


<div class="card-body">
                  <h4 class="card-title">Add Campaign</h4>
				  
               <% include ./partials/messages %>

                  <form class="forms-sample" action="<%= typeof campaign != 'undefined' ? '/campaigns/update/'+campaign._id : '/campaigns/add-new ' %>" method="POST">
                  
                    <div class="form-group">
                      <label for="exampleInputUsername1">Name</label>
                      <input type="text" class="form-control" name = "name"  value="<%= typeof campaign != 'undefined' ? campaign.name : '' %>" placeholder="Name">
                    </div>

                    <div class="form-group">
                      <label for="exampleInputUsername1">Leads</label>
                      <input type="text" class="form-control" name = "leads"  value="<%= typeof campaign != 'undefined' ? campaign.leads : '' %>" placeholder="Leads">
                    </div>


                    <div class="form-group">
                      <label for="exampleInputUsername1">Connected</label>
                      <input type="text" class="form-control" name = "connected"  value="<%= typeof campaign != 'undefined' ? campaign.connected : '' %>" placeholder="Connected">
                    </div>


                    <div class="form-group">
                      <label for="exampleInputUsername1">Closed</label>
                      <input type="text" class="form-control" name = "closed"  value="<%= typeof campaign != 'undefined' ? campaign.closed : '' %>" placeholder="Closed">
                    </div>

                     <div class="form-group">
                      <label for="exampleInputUsername1">Tottal Sales </label>
                      <input type="text" class="form-control" name = "tottal_sales" value="<%= typeof campaign != 'undefined' ? campaign.tottal_sales : '' %>" placeholder="Tottal Sales ">
                    </div>





                    <button type="submit" class="btn btn-primary mr-2">Submit</button>
                
                  </form>
                </div>
				
      <!-- main-panel ends -->
</div></div></div>
               <% include ./partials/footer %> 
